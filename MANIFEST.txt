CIBOS / CIBIOS / HIP — FULL sandbox archive (steps 1-5 + security/platform arc)
================================================================================
Product loop (runtime-verified, bare-first): boot -> persistent CIBOSFS -> login
(real crate) -> shell (real dispatch, ring 3) -> compose existing apps pkg/kv/edit
-> install from LOCAL /repo (SHA-256 verified, network-free) -> file in /apps;
flavor-selectable app baking (--with-apps).

THIS ARC added:
* SECURITY (E): firmware image verification now DISPATCHES on the image's
  signature_algorithm field (via shared verify_with, whose SPHINCS+ arm prefers
  std then the no_std portable verifier so it links bare). Fail-closed guarantee
  TESTED: an image selecting an algorithm with no compiled verifier is rejected,
  never booted unverified. cibios 27/0; bare firmware builds x86_64/aarch64/
  riscv64; QEMU Standard image boots "image verified" via the new dispatch.
  SECURITY-NOTES.md updated with honest scheme-selection status (SPHINCS+ bare +
  default; ML-DSA needs a portable verifier; Ed25519 intentionally unavailable).
* PLATFORMS (C): platform-gui and platform-mobile converted to no_std + alloc;
  both build host AND bare on all three arches. The text-cell GUI Surface +
  touch/gesture layer are now kernel-ready across the arch matrix. Remaining:
  a display+input driver to render on hardware; platform-server (kernel System).

Verified: 318 tests / 0 fail; examples build; clippy clean; on-kernel app crates
+ platform-gui/mobile build for the bare arches. See PORT-PLAN-AND-REVIEW.md for
the authoritative plan + POST-STEP-5 ROADMAP (with E and C progress + remaining
app ports: 13 host-only apps still to connect). License: MIT.
