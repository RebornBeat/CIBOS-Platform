CIBOS / CIBIOS / HIP — sandbox archive
=======================================
Full canonical workspace (41 crates) + design notes + fresh firmware ELFs.

This archive adds TRACK 3A — the Lattice (Gate/Link/Warden) on the kernel,
runtime-verified:

  1. Kernel GateRegistry (cibos-kernel/src/gate.rs, host-tested, 7 tests):
     bind/unbind/connect/accept/probe + a boundary-aware Warden (deny is TOTAL —
     blocks bind AND connect). A Link is ONE canonical Channel (both halves share
     its Arc inner) — REUSES the channel system, no parallel IPC. Resolved the
     ChannelId visibility question (it lives in `shared`, pub).

  2. Net syscalls 23-30 (GateBind/GateConnect/GateAccept/LinkSend/LinkRecv/
     LinkClose/WardenSet/GateProbe): shared enum + from_number; cibos-kernel
     SyscallEnv trait methods + dispatch arms (gate range-checked; caller boundary
     from the trap); KernelSyscallEnv impls backed by the GateRegistry, Links
     registered in the SAME handle table as channels. 3 dispatch tests.

  3. Ring-3 SDK wrappers (cibos-app/src/net.rs): bind→Listener, connect→Link,
     Listener::accept, Link::send/recv/close, warden_set, probe. Matches the SDK
     Lattice vocabulary so the NIC layer can sit beneath the same surface.

  4. Lattice QEMU demo (kernel-image/ring3.rs demonstrate_lattice), runtime-
     verified with S=0x300 (binds gate 80) / C=0x400 (connects):
       - Warden-denied gate 81: bind REFUSED + connect REFUSED (total denial)
       - probe Closed -> (bind) -> Open
       - connect -> accept (two halves of ONE Channel)
       - C 'GET /' -> S received it; S '200 OK' -> C received it (bidirectional)
     Runs alongside the cross-boundary channel handshake demo in one boot.

Prior verified milestones in this archive: per-lane ring-3 context save/resume;
selector-owned multi-lane dispatch; spawn + arg; channel unification;
cross-boundary channel handshake (syscalls 18-22); F1 live login->shell session.

Status at archive time:
  * 347 workspace tests passing, 0 failing.
  * Clippy clean; default + interactive-session + ring3-multilane-demo + aarch64 +
    riscv64 + cibos-app all build clean; i686 via build-std.
  * compute profile boots in QEMU and runs: the cross-boundary handshake demo,
    the Lattice (Gate/Link/Warden) demo, spawn+arg, and the .capp app flow.

Firmware ELFs (fresh): cibios-{x86_64,aarch64,riscv64gc,i686}.elf.
Key docs: FINALIZATION-INVENTORY.md (full cross-platform capability map + the
languages/JS analysis + per-platform app list), TRACK3-LATTICE-PLAN.md (3A done;
3B = NetDevice + virtio-net next), F1-INTERACTIVE-SESSION-PLAN.md.

Next: Track 3B — NetDevice trait + virtio-net driver (real interface, QEMU-
verifiable) + smoltcp TCP/IP beneath the Lattice (same surface, apps unchanged).
