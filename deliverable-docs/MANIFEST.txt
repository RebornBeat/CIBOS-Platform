CIBOS / CIBIOS / HIP — sandbox archive
=======================================
Full canonical workspace (41 crates) + design notes + fresh firmware ELFs.

This archive: GUI display-driver promotion (cleanup tail complete) + Track 3B
foundation (contiguous DMA frames for the virtqueues).

  1. GUI display driver promoted to production:
     - kernel-image/src/gui.rs (Surface→VGA) + arch/vga.rs confirmed always-
       compiled production; boot block reframed from "demo" to a production GUI
       surface launch (notepad as the app choice on the production runner).
     - Runner input wait: busy-spin → production hlt-wait (timer::wait_for).
       Verified in QEMU: GUI surface starts and hlt-blocks for live input.
     - Confirmed run_interactive_session (live login→gated shell) is the
       production interactive posture; injected path is selftest-only.
     => PRODUCTION CLEANUP COMPLETE (see PRODUCTION-CLEANUP-AUDIT.md).

  2. Track 3B foundation (TRACK3B-VIRTIO-TXRX-PLAN.md):
     - FrameAllocator::allocate_contiguous(n) — allocates n physically-contiguous
       frames, the DMA-memory primitive virtio virtqueues require (identity map:
       phys==virt). +2 host tests. This unblocks the virtqueue TX/RX work (B3a-d),
       which is fully designed in the plan (legacy split virtqueue layout, the
       virtio-pci I/O registers, the 10-byte virtio_net_hdr, RX fill/poll + TX).

Status at archive time:
  * 355 workspace tests passing, 0 failing.
  * Clippy clean; production(default) + gui-demo + interactive-session +
    multilane + storage-selftest + channel-demo + ring3-resume-demo + aarch64 +
    riscv64 all build clean; i686 via build-std.
  * Production image boots in QEMU (real handoff path): .capp in ring 3, channel/
    Lattice table installed, NIC probed (real MAC), GUI surface hlt-blocks.

Firmware ELFs (fresh): cibios-{x86_64,aarch64,riscv64gc,i686}.elf.

Next: Track 3B B3a — allocate + set up the RX (queue 0) and TX (queue 1)
virtqueues via allocate_contiguous + QUEUE_SELECT/QUEUE_PFN, then RX/TX paths,
then QEMU -filter-dump verification; then B4 (NIC under the Lattice via smoltcp).
