CIBOS / CIBIOS / HIP — sandbox archive
=======================================
Full canonical workspace (41 crates) + design notes + fresh firmware ELFs.

This archive: Track 3 (Lattice on the kernel) + the virtio-net driver made
PRODUCTION CODE (the key correction this session).

  1. Lattice on the kernel (Gate/Link/Warden over syscalls), loopback-backed:
     - cibos-kernel/src/gate.rs: GateRegistry — bind/connect/accept/probe + a
       boundary-aware Warden whose denial is TOTAL (blocks bind AND connect).
       Links REUSE the canonical Channel (no parallel IPC). 7 host tests.
     - shared: net syscalls 23-30 (GateBind/GateConnect/GateAccept/LinkSend/
       LinkRecv/LinkClose/WardenSet/GateProbe) + dispatch + trait + KernelSyscall
       Env impls. cibos-app/src/net.rs ring-3 wrappers. +3 dispatch tests.
     - QEMU-verified: Warden-denied gate refuses bind+connect; probe Closed→Open;
       connect→accept = two halves of one Channel; bidirectional bytes (GET / →
       200 OK) across boundaries through the kernel.

  2. NetDevice trait (cibos-kernel/src/net_device.rs) mirroring BlockDevice:
     portable trait + coarse errors + in-memory LoopbackNic test impl. 6 tests.

  3. virtio-net driver (kernel-image/src/arch/virtio_net.rs) — REAL driver for a
     REAL interface (virtio-pci spec: PCI config 0xCF8/0xCFC, vendor 0x1AF4/dev
     0x1000, the spec's init handshake, MAC+status read). NOT QEMU-specific.

  4. PRODUCTION-CORRECTNESS FIX (the important one): the virtio-net driver was
     wrongly gated behind a `virtio-net-demo` feature (built for the demo, not
     production). Now it is `pub mod virtio_net` — ALWAYS compiled on x86_64 and
     probed at boot by `probe_nic_at_boot()`, exactly like the ATA storage probe.
     `virtio-net-demo` now only adds a verbose log line. VERIFIED with the
     PRODUCTION image (no demo feature): NIC present → real MAC 52:54:00:ab:cd:ef
     read (matches the QEMU-assigned MAC — unfakeable); NIC absent → honest
     "loopback only" fallback. Principle locked: drivers are production code
     probed at boot; QEMU/cloud/bare-metal merely VERIFY them.

Status at archive time:
  * 353 workspace tests passing, 0 failing.
  * Clippy clean; default(production) + virtio-net-demo + interactive-session +
    ring3-multilane-demo + aarch64 + riscv64 build clean; i686 via build-std.
  * compute profile boots in QEMU and runs the Lattice demo, the cross-boundary
    channel handshake, spawn+arg, the gated login→shell session, AND the
    production NIC probe (real MAC), then boot complete.

Firmware ELFs (fresh): cibios-{x86_64,aarch64,riscv64gc,i686}.elf.

Next (Track 3B): e1000 driver (bare-metal boxes without virtio); virtio-net
TX/RX over the virtqueues; wire the NIC under the Lattice's NIC-backed transport
(same Gate/Link/Warden surface, apps unchanged). Then Surface v2, CLI app ports,
on-kernel GUI, per-arch ring-3, mobile, new apps, scripting/Boa, Track 4.
